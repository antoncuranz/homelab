---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: envoy-gateway
  namespace: networking
spec:
  interval: 1h
  install:
    crds: Skip
  upgrade:
    crds: Skip
  chartRef:
    kind: OCIRepository
    name: envoy-gateway
